/**
 * Central type documentation (JSDoc). Import for side effects only.
 * @see ./campaign.js — Campaign, Session, Scene, Beat, StatBlock, Spell, NPC, Encounter, …
 * @see ./runtime.js — PlayerState, CombatStateRow, RevealState
 */
import './campaign.js'
import './runtime.js'

export {}
