/**
 * CLI / Node re-export — canonical implementation lives in shared.
 * @deprecated Import from shared/lib/parseSessionMarkdown.js in new code.
 */
export {
  parseSessionMarkdown,
  parseDocxSession,
} from '../shared/lib/parseSessionMarkdown.js'
