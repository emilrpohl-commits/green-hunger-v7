# green-hunger-v7

Practice PR: repository and branch workflow validated.

## CI

GitHub Actions: [`.github/workflows/ci.yml`](.github/workflows/ci.yml) — lint, typecheck, build (DM + players), tests (players + shared). Configure repo secrets `VITE_SUPABASE_URL` / `VITE_SUPABASE_KEY` for the Pages workflow build.

